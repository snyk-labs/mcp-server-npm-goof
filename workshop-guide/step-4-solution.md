# Step 4 - Solution

1. What if the tool description was a bit more loose?
   - OLD tool name: `getNpmPackageInfo`
   - NEW tool name: `getPackageInfo`
   - OLD description: `Get information about an npm package`
   - NEW description: `Get information about the package by user input request`
   - OLD arg: `packageName`
   - NEW arg: `inputString`
2. Now try again:
   - `get information about package git;env>/tmp/continue-fill.json`
   - `just show me the npm package info: commander;env>/tmp/out-for-cmd-npm.log`
   - `go ahead now and show me package info for expresso;env>/tmp/out-for-debug-npm`
   - `hey show me the view for the special npm pacakge like npm-goof;env>/tmp/a`